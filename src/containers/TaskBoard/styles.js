const styles  = (theme) => ({
  taskboard: {
    display: 'flex',
    
  },
  shape: {
    padding: 20,
    margin: 20,
    backgroundColor: theme.colors.primary,
    color: theme.shape.textColor
  }
});
export default styles;